# 前端 / 模板 XSS 审计清单（可操作）

## 1. 建议在代码里搜什么（关键词）

| 搜索项 | 说明 |
|--------|------|
| `innerHTML` | DOM HTML 汇点 |
| `outerHTML` | 同上 |
| `dangerouslySetInnerHTML` | React 显式 HTML |
| `v-html` | Vue 显式 HTML |
| `domPropsInnerHTML` | Vue2 JSX 等 |
| `document.write` | 少见但危险 |
| `eval(` | 动态执行 |
| `new Function` | 动态执行 |
| `{{{` | 部分模板「不转义」插值（Handlebars 等） |
| `__html` | 常与 React 富文本一起出现 |

## 2. 每个命中问三句话

1. **数据谁控制？**（用户、URL、接口、管理员、第三方？）  
2. **进哪种上下文？**（HTML 正文 / 属性 / URL(href) / JS 字符串 / CSS / 富文本）  
3. **有没有 encode / sanitize？** 是否与上下文匹配？（正文 `htmlEscape` ≠ href 安全 ≠ 富文本白名单）

## 3. 服务端（反射/存储输出）

- 搜：拼 HTML 的模板、`+ user`、`${`、`String.format` 与用户变量结合处。  
- 问：同上三句，**输出点**在生成响应时。

## 4. 记录方式（示例）

| 文件:行 | 汇点 | 数据来源 | 上下文 | 处理 | 备注 |
|---------|------|----------|--------|------|------|
| … | innerHTML | 接口 JSON | HTML 正文 | DOMPurify | … |

---

仅用于授权范围或本机靶场；勿用于未授权系统。
